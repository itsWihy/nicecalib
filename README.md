# 📹 Camera Calibration and Video Processing Web Application 📹
This project is a Flask web application designed for camera calibration and video processing. It provides a user-friendly interface for uploading videos, calibrating cameras, and processing video frames. The application is built using a range of technologies, including Flask, OpenCV, and NumPy, and is designed to be scalable and efficient.

## 🚀 Key Features
* **Camera Calibration**: The application allows users to calibrate cameras using a set of predefined configuration parameters.
* **Video Processing**: The application can process video frames using OpenCV and NumPy.
* **Video Uploads**: Users can upload videos in a range of formats, including MP4, AVI, and MOV.
* **Camera Profiles**: The application includes a range of predefined camera profiles, each with its own set of configuration parameters.
* **Configurable**: The application is highly configurable, with users able to adjust settings such as frame rate, resolution, and compression.

## 🛠️ Tech Stack
* **Frontend**: Flask
* **Backend**: Flask
* **Database**: None
* **AI Tools**: OpenCV
* **Build Tools**: None
* **Libraries**: NumPy, tempfile, os, sys, platform, json, threading, time, and math

## 📦 Installation
To install the application, follow these steps:
### Prerequisites
* Python 3.8 or later
* Flask 2.0 or later
* OpenCV 4.5 or later
* NumPy 1.20 or later
### Installation
1. Clone the repository using `git clone https://github.com/your-repo/camera-calibration.git`
2. Navigate to the repository directory using `cd camera-calibration`
3. Install the required dependencies using `pip install -r requirements.txt`
### Running Locally
1. Run the application using `python main.py`
2. Open a web browser and navigate to `http://localhost:5000`

## 💻 Usage
To use the application, follow these steps:
1. Upload a video file using the upload form
2. Select a camera profile from the dropdown menu
3. Adjust the configuration parameters as needed
4. Click the "Calibrate" button to calibrate the camera
5. Click the "Process" button to process the video frames

## 📂 Project Structure
```markdown
camera-calibration/
|---- main.py
|---- templates/
|       |---- index.html
|---- static/
|       |---- css/
|       |---- js/
|---- requirements.txt
|---- README.md
```

## 📸 Screenshots

## 🤝 Contributing
To contribute to the project, please follow these steps:
1. Fork the repository using `git fork https://github.com/your-repo/camera-calibration.git`
2. Create a new branch using `git branch feature/new-feature`
3. Make your changes and commit them using `git commit -m "New feature"`
4. Push the changes to the remote repository using `git push origin feature/new-feature`
5. Create a pull request using the GitHub web interface

## 📝 License
The project is licensed under the MIT License.

## 📬 Contact
For more information, please contact us at [your-email@example.com](mailto:your-email@example.com). 
